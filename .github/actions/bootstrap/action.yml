name: Bootstrap
description: Sets up the environment with Rust and Cairo native dependencies

runs:
  using: composite
  steps:
    - name: Install rust
      uses: ./.github/actions/install_rust

    - name: Install cairo native
      uses: ./.github/actions/setup_native_deps
